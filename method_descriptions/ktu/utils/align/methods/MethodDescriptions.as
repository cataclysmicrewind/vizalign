package {
	import flash.display.Sprite;
	
	/**
	 * 
	 * This swf explains how each method works.
	 * 
	 * once it is done, you should only have to modify the xml to add more options
	 * 
	 * 
	 * 
	 * 	So, place the overlay, loading data
	 * if fail, show fail
	 * 
	 * on data
	 * parse it and create ui
	 * 
	 * once settled, just handle button interactions
	 * 
	 * 
	 * 
	 * ...
	 * @author ktu
	 */
	public class MethodDescriptions extends Sprite {
		
		public function MethodDescriptions() {
		
		}
	
	}

}